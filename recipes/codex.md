# OpenAI Codex Recipe

Install Codex inside WSL:

```bash
npm install -g @openai/codex
codex login
```

Verify:

```bash
codex --version
```

Recommended usage:

```bash
cd ~/code/my-project
codex
```
