# Claude Code Recipe

Install Claude Code inside WSL:

```bash
npm install -g @anthropic-ai/claude-code
claude login
```

Verify:

```bash
claude --version
```

Recommended usage:

```bash
cd ~/code/my-project
claude
```
